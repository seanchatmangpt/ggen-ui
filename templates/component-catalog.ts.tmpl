---
to: "app/generated/component-catalog.ts"
sparql:
  components: |
    PREFIX ui: <https://ggen.io/ontology/ggen-ui#>
    SELECT ?id ?label ?description ?icon
    WHERE {
      ?component a ui:ComponentKind ;
                 ui:componentId ?id ;
                 ui:label ?label ;
                 ui:description ?description ;
                 ui:icon ?icon .
    }
    ORDER BY ?id
---
/* GENERATED BY ggen FROM ontology/ggen-ui.ttl. DO NOT EDIT. */
import type { ComponentDefinition } from '~/types/ggen-ui'

export const COMPONENT_CATALOG = [
{% for component in components %}  {
    kind: "{{ component.id }}",
    label: "{{ component.label }}",
    description: "{{ component.description }}",
    icon: "{{ component.icon }}"
  },
{% endfor %}] as const satisfies readonly ComponentDefinition[]
